# FormIQ — AI-Powered Gym Exercise Form Assessment
# Install: uv pip install -r requirements.txt

# ── Core AI / CV ──────────────────────────────────────────────────────────────
protobuf==3.20.3            # pinned — mediapipe breaks with protobuf 4.x+
mediapipe==0.10.9
opencv-python-headless>=4.8.0  # headless avoids AVFFrameReceiver dylib conflict on macOS
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
joblib>=1.3.0

# ── Deep learning (used by mediapipe internally) ───────────────────────────────
torch>=2.0.0
torchvision>=0.15.0

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit>=1.30.0

# ── Live camera in browser (optional — skip if protobuf conflicts persist) ────
# streamlit-webrtc>=0.47.0
# aiortc>=1.6.0
# av>=10.0.0